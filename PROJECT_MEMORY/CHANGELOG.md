# CHANGELOG

سجل جميع التعديلات المهمة والميزات المضافة للإبقاء على ذاكرة المشروع محدثة.

## [2026-08-29] - تحسينات الأمان والتحقق الصارم من كود OTP وتسريع أتمتة تطبيق الأندرويد والربط السحابي (100/100)

### 1. إصلاح التحقق الصارم من رمز OTP ومنع التجاوز في الواجهة (`ProfileForm.tsx`)
- **المشكلة السابقة:** عند كتابة كود تفعيل غير صحيح والضغط على زر `Enter` أو `حفظ التغييرات`، كان النموذج يتم إرساله ويتم تحويل المستخدم إلى الصفحة الرئيسية بالخطأ دون توثيق الرقم.
- **التعديل:**
  - إضافة معالج `onKeyDown` لحقل إدخال رمز التحقق لمنع الإرسال التلقائي للنموذج واستدعاء دالة `handleVerifyCode()` حصرياً عند الضغط على Enter.
  - إلزام دالة `onSubmit` بعدم السماح بالحفظ أو التحويل إذا كان هناك كود تحقق مُرسل (`codeSent === true`) إلا بعد التحقق والتوثيق بنجاح، وإظهار تنبيهات باللون الأحمر في حالة الكود الخاطئ.
- **الملفات المتأثرة:** `src/components/ProfileForm.tsx`.

### 2. تسريع وضبط أتمتة الإرسال التلقائي في تطبيق الأندرويد (`My-otp`)
- **المشكلة السابقة:** حدوث بطء في الخيط الرئيسي وتجاوز المهلة الزمنية أحياناً بسبب البحث الشجري الثقيل.
- **التعديل:**
  - اعتماد تقنية **Entry-Sibling Detection** الفورية في `WhatsAppAccessibilityService.java` للعثور على حقل الكتابة `com.whatsapp.w4b:id/entry` والضغط الفوري على زر الإرسال المجاور في أقل من 1 مللي ثانية.
  - ضبط استجابة إمكانية الوصول إلى `50ms` وتفعيل `flagReportViewIds` في `accessibility_service_config.xml`.
- **الملفات المتأثرة:** `WhatsAppAccessibilityService.java`, `res/xml/accessibility_service_config.xml`.

### 3. الربط السحابي الدائم للموقع الحي عبر Cloudflare Tunnel
- **التعديل:**
  - إعداد نفق سحابي مشفر HTTPS عالي السرعة يربط خوادم Firebase السحابية بتطبيق بوابة الأندرويد على الهاتف مباشرة.
  - تحديث قائمة الـ Gateway Candidates في `src/lib/whatsapp-gateway-client.ts` وإضافة سكريبت التشغيل السريع `start-phone-tunnel.bat`.
- **الملفات المتأثرة:** `src/lib/whatsapp-gateway-client.ts`, `.env.production`, `start-phone-tunnel.bat`, `PROJECT_MEMORY/bug-history/BUG-006-whatsapp-otp-auto-click-and-form-bypass.md`.

---

### 1. تكامل مباشر مع تطبيق بوابة الرسائل SMSGatewayApp (My-otp)
- **التعديل:** تمكين الموقع من إرسال رسائل التحقق النصية القصيرة (SMS OTP) الحقيقية والمجانية مباشرة من شريحة هاتف الأندرويد (SIM Card) عبر تطبيق `My-otp`.
- **التفاصيل:**
  - إنشاء وحدة الربط `src/lib/sms-gateway-client.ts` للاتصال مع خادم التطبيق على المنفذ `8080` مع دعم التوكن الأمني وتشفير `HMAC-SHA256`.
  - إنشاء مسارات الـ API:
    - `/api/auth/sms-gateway/send`: توليد وحفظ كود التحقق وإرساله إلى الهاتف.
    - `/api/auth/sms-gateway/verify`: التحقق السريع من كود الـ OTP وتوثيق رقم الهاتف.
  - تحديث `src/components/ProfileForm.tsx` ليكون الإرسال الافتراضي عبر بوابة رسائل الأندرويد SMS مع دعم تلقائي للبدائل (واتساب و Firebase).
- **الملفات المتأثرة:** `src/lib/sms-gateway-client.ts`, `src/app/api/auth/sms-gateway/send/route.ts`, `src/app/api/auth/sms-gateway/verify/route.ts`, `src/components/ProfileForm.tsx`, `.env.local`.

---

## [2026-08-27] - تفعيل رسائل كود التحقق OTP الأساسية عبر Firebase Phone Auth مع خيار واتساب

### 1. تفعيل إرسال كود التحقق عبر رسائل SMS من Firebase (Firebase Phone Auth)
- **التعديل:** إعادة تفعيل إرسال أكواد التحقق (OTP) عبر رسائل SMS المباشرة من فايربيس (Firebase Phone Auth) كخيار أساسي وموثوق.
- **التفاصيل:**
  - تحديث `src/components/ProfileForm.tsx` لدعم خيار التحقق من Firebase عبر `sendVerificationCode` و `RecaptchaVerifier`.
  - توفير زرين واضحين للمستخدم: **تأكيد برمز SMS** الأساسي، وزر اختياري بديل **عبر واتساب**.
  - دعم التحقق المزدوج تلقائياً عبر `confirmVerificationCode` لرسائل فايربيس أو عبر الـ Gateway لرسائل واتساب.
- **الملفات المتأثرة:** `src/components/ProfileForm.tsx`, `src/context/AuthContext.tsx`, `src/app/globals.css`.

---

## [2026-08-27] - إصلاح مشكلة حذف الصور القديمة عند تعديل أو إضافة صور جديدة للإعلان

### 1. إصلاح الاحتفاظ بالصور القديمة عند إضافة صور جديدة (Image Retention on Ad Edit)
- **المشكلة السابقة:** عند تعديل إعلان وإضافة صورة جديدة إليه، كانت دالة التحديث `updateAd` تقوم بمسح كافة الصور القديمة من الـ Storage واستبدالها بالصورة الجديدة فقط مع ظهور رسالة "جارٍ حذف الصور القديمة...".
- **التعديل:**
  - تعديل دالة `updateAd` في `src/context/AuthContext.tsx` لمعالجة قائمة الصور المدمجة (`rawImages`) بحسب ترتيبها الفعلي.
  - رفع الصور والملفات الجديدة فقط ودمجها مع الصور القديمة المحتفظ بها بنفس ترتيب اختيار المستخدم.
  - قصر الحذف من الـ Storage على الصور التي قام المستخدم بإزالتها فعلياً من الإعلان فقط، وعدم مسح أي صورة اختار المستخدم إبقاءها.
- **الملفات المتأثرة:** `src/context/AuthContext.tsx`.

---

## [2026-08-27] - قصر ميزات الفيديو والموقع على الباقة الذهبية وتحديد الوصف بـ 2000 حرف

### 1. قصر ميزات إضافة رابط الفيديو والموقع الخارجي على الباقة الذهبية
- **التعديل:** جعل مربع الميزات الخاصة بروابط الفيديو والمواقع الإلكترونية متاحاً حصرياً لمشتركي الباقة الذهبية (`gold`) والمشرفين فقط، وإظهار شارة ودعوة للترقية لغير المشتركين.
- **الملفات المتأثرة:** `src/components/AdForm.tsx`.

### 2. تحديد عدد أحرف وصف الإعلان بـ 2000 حرف
- **التعديل:** تحديث مخطط التحقق Zod ليكون الحد الأقصى 2000 حرف مع إضافة عداد أحرف تفاعلي يوضح المتبقي للمستخدم (`X / 2000`).
- **الملفات المتأثرة:** `src/components/AdForm.tsx`.

---

## [2026-08-27] - نشر بوابة واتساب السحابية على Google Cloud Run وحل خطأ إرسال كود التفعيل (Fetch Failed)

### 1. نشر بوابة واتساب على Google Cloud Run كخدمة سحابية دائمة (24/7)
- **التعديل:**
  - إنشاء ملف `whatsapp-gateway/Dockerfile` و `.dockerignore` لتشغيل خادم Node.js وبوابة Baileys على بيئة حاويات سحابية.
  - إطلاق الخدمة السحابية على Google Cloud Run في منطقة `europe-west1` وضبط `Min instances = 1` و `Max instances = 1` لضمان استمرار اتصال WebSocket وجلسة واتساب بدون انقطاع.
  - ربط حساب واتساب المعتمد (`+201003975823`) بمسح الـ QR السحابي.
- **السبب:** حل مشكلة فشل إرسال أكواد التفعيل والرسائل (`fetch failed`) بعد نشر الموقع، حيث كان الموقع يحاول الاتصال بـ `localhost:5005` غير المتوفر في بيئة السحابة.
- **الملفات المتأثرة:** `whatsapp-gateway/Dockerfile`, `whatsapp-gateway/.dockerignore`, `src/lib/whatsapp-gateway-client.ts`, `.env.production`, `.env.local`, `PROJECT_MEMORY/bug-history/BUG-005-whatsapp-gateway-cloud-run.md`.

### 2. تحسين معالجة أخطاء اتصال بوابة واتساب ورسائل التنبيه
- **التعديل:** تحديث دوال العميل في `src/lib/whatsapp-gateway-client.ts` لتعيين رابط Cloud Run السحابي كافتراضي، وتحويل رسائل الأخطاء التقنية الجافة إلى رسائل عربية واضحة ومباشرة.
- **السبب:** تحسين تجربة المستخدم والإدارة لتوضيح حالة الاتصال بدقة في حال انقطاع البوابة أو الحاجة لربط الجهاز.
- **الملفات المتأثرة:** `src/lib/whatsapp-gateway-client.ts`.

---


### 1. حل مشكلة اختفاء الصور بعد النشر (Image Optimization on Firebase Hosting)
- **التعديل:** إعادة تمكين خاصية `unoptimized: true` في قسم `images` بداخل ملف `next.config.ts`.
- **السبب:** لا تدعم وظائف Firebase Cloud Functions المخصصة لاستضافة Next.js معالجة الصور ديناميكياً بدون وجود Sharp أو تهيئة معقدة، مما يؤدي لظهور أخطاء (broken images) وتوقف عرض الصور بعد الرفع بينما تعمل محلياً بشكل سليم. تفعيل `unoptimized: true` يسمح بتحميل الصور مباشرة من Firebase Storage دون وساطة معالجة سيرفرية.
- **الملفات المتأثرة:** `next.config.ts`.

### 2. تفعيل تسجيل الدخول التلقائي بنقرة واحدة (Google One Tap)
- **التعديل:**
  - إنشاء مكون عميل جديد `GoogleOneTap.tsx` وربطه في `layout.tsx`.
  - تحديث سياسة أمان المحتوى (Content Security Policy) في `next.config.ts` لإضافة `https://accounts.google.com` إلى توجيهات `script-src` و `connect-src` و `frame-src`.
  - وضع معرّف العميل `NEXT_PUBLIC_GOOGLE_CLIENT_ID` في ملف البيئة `.env.local`.
- **السبب:** تلبية رغبة المستخدم في تبسيط الدخول للموقع لجذب أكبر عدد من الزوار.
- **الملفات المتأثرة:** `src/components/GoogleOneTap.tsx`, `src/app/layout.tsx`, `next.config.ts`, `.env.local`.

### 3. نظام توثيق الحسابات التفاعلي والربط بالترقيات
- **التعديل:**
  - إدراج خيار توثيق الحساب الاختياري في صفحة تعديل الملف الشخصي `ProfileForm.tsx`.
  - حظر ترقية الإعلانات في صفحة الأسعار `pricing/page.tsx` للأعضاء غير الموثقين وتوجيههم لصفحة الإعدادات للتوثيق أولاً.
  - إظهار شارات التحقق الزرقاء بجوار الاسم في كروت الإعلانات `AdCard.tsx` وتفاصيل الإعلان `AdDetailClient.tsx` والملف الشخصي العام.
- **السبب:** تلبية وتوثيق رغبة المستخدم في تحفيز الأعضاء على التوثيق عند الرغبة في الترقية لزيادة المصداقية.
- **الملفات المتأثرة:** `src/components/ProfileForm.tsx`, `src/app/pricing/page.tsx`, `src/components/AdCard.tsx`, `src/components/AdDetailClient.tsx`, `src/app/profile/[id]/page.tsx`, `src/lib/types.ts`.

### 4. إصلاح أخطاء رسم أيقونات SVG (WhatsApp Icon Path Fixes)
- **التعديل:** استبدال كود أيقونة الواتساب `WhatsappIcon` الطويل والمعقد بكود مسار رسم (SVG Path) قياسي وموثوق وخالٍ من الأخطاء.
- **السبب:** تفادي تعطل واجهة كرت الإعلان في المتصفحات بسبب تعذر قراءة حروف مسار الرسم.
- **الملفات المتأثرة:** `src/components/AdCard.tsx`, `src/components/AdDetailClient.tsx`.

---

## [2026-08-12] - تحسين أداء الموقع ومؤشرات PageSpeed Insights وإصلاح سباق تصفية الدول

### 1. تحسين LCP وتقليل تأخير تحميل الصورة الرئيسية (Largest Contentful Paint)
- **التعديل:** ربط خاصية `priority` في `AdCard.tsx` والمكون `HomeClient.tsx`. يتم إعطاء أول كارتين في القائمة السمة `priority={idx < 2}` مما يضيف `fetchpriority="high"` في Next.js ويصرف المتصفح عن تأجيل تحميل الصورة عبر `loading="lazy"`.
- **السبب:** رفع نتيجة أداء LCP وتسريع عرض المحتوى البصري الرئيسي المباشر للزائر وفق توصيات Lighthouse.
- **الملفات المتأثرة:** `src/components/AdCard.tsx`, `src/app/HomeClient.tsx`.

### 2. تحسين اتصالات الشبكة المسبقة (Preconnect Optimization)
- **التعديل:** حذف الروابط غير المستخدَمة لـ Google Fonts و Cloudinary من الـ `<head>` في `src/app/layout.tsx` وإضافة `preconnect` مباشر لـ `firestore.googleapis.com` و `arb-soq.firebaseapp.com`.
- **السبب:** توفير من 300ms إلى 350ms في زمن إنشاء الاتصال الأولي (TLS handshake) ومنع الاستهلاك غير الضروي لموارد الشبكة.
- **الملفات المتأثرة:** `src/app/layout.tsx`.

### 3. حل مشكلة الـ Stale Closure وتزامن تصفية إعلانات دولة السعودية
- **التعديل:** تمرير كائن `currentMarket` صراحة لدالة التصفية `sortAndSetAds` في `HomeClient.tsx`.
- **السبب:** ضمان تحديث وتجميع إعلانات السعودية والدول فوراً عند اختيارها من شريط التنقل واستقرارها التام عند إعادة التحميل (`Refresh`).
- **الملفات المتأثرة:** `src/app/HomeClient.tsx`.

---

## [2026-08-11] - الحل الجذرائي لمشكلة توقف ومهلة الرفع (Firebase Deploy Timeout Fix)

### 1. توحيد وإصلاح حزم Genkit و Firebase Functions
- **التعديل:** توحيد إصدارات جميع حزم Genkit (`genkit`, `@genkit-ai/firebase`, `@genkit-ai/googleai`, `@genkit-ai/next`, `@genkit-ai/google-cloud`) للإصدار `1.41.0` وترقية `firebase-functions` للإصدار `7.3.2`.
- **السبب:** إزالة التعارض الجذري في شجرة الاعتماديات وأخطاء `ELSPROBLEMS` وتسريع التحميل الأولي للموديولات على السيرفر.
- **الملفات المتأثرة:** `package.json`, `package-lock.json`.

### 2. تحسين وتنظيف إعدادات البناء والرفع
- **التعديل:** إزالة `output: 'standalone'` ومصفوفة `outputFileTracingExcludes` من `next.config.ts` لترك تتبع البناء لـ Firebase CLI دون تضارب. رفع الذاكرة لـ `1GiB` وتحديد `minInstances: 0` في `firebase.json`. إضافة سكريبت الرفع الآمن `npm run deploy` مع `FUNCTIONS_EMULATOR_TIMEOUT=60s` وخيار `--force`.
- **السبب:** ضمان إنهاء فحص واستجابة الدالة السحابية بدون أي توقف نهائياً.
- **الملفات المتأثرة:** `next.config.ts`, `firebase.json`, `package.json`, `PROJECT_MEMORY/bug-history/BUG-002-firebase-deploy-timeout.md`.

## [2026-08-11] - عزل وتصفية إعلانات الدول، الروابط الحديثة /p/slug، وتحديثات الفوتر وتفاصيل الإعلانات

### 1. عزل وتصفية إعلانات الدول عبر أيقونة علم الدولة العلوية
- **التعديل:**
  - اعتماد دالة `isAdInMarket(ad, market.id, market.name.ar)` في `src/lib/category-utils.ts` لربط الإعلانات بدقة متناهية بالدولة المحددة في رأس الموقع.
  - تفحص الدالة معرّف السوق (`eg`, `sa`) والعملات والأسماء العربية لمصر والسعودية وكافة المحافظات والمدن، وترجع `false` بشكل قاطع لأي إعلان ينتمي لدولة أخرى غير دولة الزائر النشطة.
  - تصفية الإعلانات في الصفحة الرئيسية `HomeClient.tsx` وفي مكون صفحات الفئات `AdPageClient.tsx`.
  - إزالة أي أزرار أو أشرطة تنبيه لجميع الدول العربية بناءً على رغبة المستخدم.
- **السبب:** لمنع تداخل أو اختلاط إعلانات مصر مع إعلانات السعودية، والاعتماد الكلي التام على خيار الدولة النشط في رأس الموقع.
- **الملفات المتأثرة:** `src/lib/category-utils.ts`, `src/app/HomeClient.tsx`, `src/components/AdPageClient.tsx`.

### 2. اعتماد الروابط الحديثة (/p/[slug]) وإلغاء الواجهات القديمة
- **التعديل:**
  - تطوير دالة `getCategorySlug` ودالة `getParentCategoryId` في `src/lib/category-utils.ts` للتحويل الفوري لأي معرف فئة (مثل `furniture`, `services`, `crafts`, `vehicles`, إلخ) إلى الرابط الحديث التفاعلي `/p/[slug]`.
  - استبدال واجهة `/category/[id]` القديمة كلياً بملف تحويل سيرفري تلقائي (`redirect`) ينقل أي طلب مثل `/category/services?sub=...&q=...` فوراً وسيرفرياً إلى `/p/professional-services?sub=...&q=...` مع الحفاظ على معايير الفلترة والبحث.
  - تحديث كافة روابط الأقسام المباشرة في `QuickOptions.tsx`, `CategoriesGridHero.tsx`, و `categories/page.tsx`.
- **السبب:** توحيد جميع صفحات الأقسام والفئات تحت التصميم المعتمد الحديث ومنع ظهور أجزاء من الواجهات القديمة.
- **الملفات المتأثرة:** `src/lib/category-utils.ts`, `src/app/category/[id]/page.tsx`, `src/components/QuickOptions.tsx`, `src/components/CategoriesGridHero.tsx`, `src/app/categories/page.tsx`.

### 3. إصلاح فتح صفحة تفاصيل الإعلان والتواصل
- **التعديل:**
  - إنشاء المسار الفردي الاحتياطي `src/app/ad/[adId]/page.tsx`.
  - تطوير دالة `getAdData` في `src/app/ad/[userId]/[adId]/page.tsx` للبحث أولاً في مجموعة `ads` الرئيسية واستخراج بيانات الناشر تلقائياً ثم البحث الاحتياطي في مجموعات المستخدمين والمتاجر.
  - تحديث المكونين `AdCard.tsx` و `AdRow.tsx` لاستخدام معرّف الناشر الحصين `effectiveUserId` وإلغاء حظر الكروت عند غياب `adUser`.
- **السبب:** ضمان فتح صفحة تفاصيل الإعلان وأزرار الاتصال والواتساب بنسبة 100% دون أخطاء 404 أو تعثر في التحميل.
- **الملفات المتأثرة:** `src/app/ad/[adId]/page.tsx`, `src/app/ad/[userId]/[adId]/page.tsx`, `src/components/AdCard.tsx`, `src/components/AdRow.tsx`.

### 4. التحديث التكاملي لهيكلية الفوتر السفلي وتصغير الأزرار
- **التعديل:**
  - وضع مربع **الإحصائيات** (عدّادات 2x2 للمسجلين والمتاجر والإعلانات والزيارات) مباشرة أسفل أيقونة وتفاصيل ووصف سوق العرب في الفوتر.
  - وضع عمود **فئات الأقسام** في الشبكة الرئيسية بين عمود سوق العرب وعمود درج صفحات الهبوط.
  - حصر قائمة **قانوني** على سياسة الخصوصية واتفاقية الاستخدام فقط وحظر ظهور أي فئات فيها.
  - حذف استيراد ومكون القائمة البريدية `FooterNewsletterDrawer` نهائياً لعدم الحاجة له.
  - تصغير أبعاد زر تحميل التطبيق `AppDownloadButton` في الهيدر والفوتر ليظهر بشكل مصغر وأنيق ومدمج.
- **السبب:** الاستجابة الكاملة لتوجيهات المستخدم وتنظيم هيكل الفوتر السفلي.
- **الملفات المتأثرة:** `src/components/Footer.tsx`, `src/components/AppDownloadButton.tsx`.

---

## [2026-08-08] - تحسينات SEO وفهرسة الإعلانات في محركات البحث

### 1. تحسين البيانات الوصفية (Metadata) لصفحات الإعلانات
- **التعديل:**
  - تحديث `generateMetadata` في `src/app/ad/[userId]/[adId]/page.tsx` لبناء `description` أغنى يجمع الوصف + الفئة + الموقع.
  - إضافة `keywords` ديناميكية تأخذ عنوان الإعلان + الفئة + المدينة + الدولة.
  - إضافة `robots: { index: true, follow: true, googleBot: { 'max-image-preview': 'large', 'max-snippet': -1 } }` لضمان قراءة جوجل للصور والمقتطفات.
  - إضافة `robots: { index: false }` على صفحات الإعلانات غير الموجودة (404) لمنع الفهرسة الخاطئة.
- **السبب:** تلبية متطلبات الفهرسة في Google Search Console وتحسين ظهور الإعلانات في نتائج البحث.
- **الملفات المتأثرة:** `src/app/ad/[userId]/[adId]/page.tsx`.

### 2. توسيع JSON-LD لجميع أنواع الإعلانات (Schema.org Structured Data)
- **التعديل:**
  - تحويل JSON-LD من كائن واحد (Product فقط) إلى مصفوفة `[Product, BreadcrumbList]`.
  - إضافة `BreadcrumbList` يضم مسار التنقل: سوق العرب ← الفئة ← الإعلان — يساعد Google على فهم هيكل الموقع.
  - تطبيق JSON-LD على **جميع** أنواع الإعلانات (صوري + فيديو + Shorts) وليس الصوري فقط.
  - إضافة `url` للبائع يشير إلى صفحة العامل.
  - تحسين `currencyMap` ليغطي جميع الدول العربية (SAR, EGP, AED, KWD, QAR, BHD, OMR, JOD).
- **السبب:** Rich Results في Google تتطلب هذه البيانات لإظهار تفاصيل المنتج/الإعلان مباشرة في صفحة نتائج البحث.
- **الملفات المتأثرة:** `src/app/ad/[userId]/[adId]/page.tsx`.

### 3. تحسين أولوية الإعلانات في الـ Sitemap
- **التعديل:**
  - رفع أولوية الإعلانات العادية من `0.6` إلى `0.75` والإعلانات المميزة من `0.8` إلى `0.9`.
  - استخدام `updatedAt` أو `createdAt` من Firestore بدلاً من `postedAt` فقط لضمان دقة `lastModified`.
- **السبب:** Google تُعطي أولوية أعلى للصفحات ذات priority أكبر في الـ sitemap وتزحف إليها بشكل أسرع.
- **الملفات المتأثرة:** `src/app/sitemap.ts`.

> **ملاحظة:** بعد هذه التعديلات يُنصح بـ:
> 1. النشر على الإنتاج (Production Deploy).
> 2. الذهاب إلى Google Search Console → طلب فهرسة يدوي (URL Inspection) للصفحات الموجودة في قائمة "تم اكتشافها - لم تُفهرس".
> 3. الانتظار 48-72 ساعة حتى تُعيد Google الزحف.

---

## [2026-08-08] - ميزة التنبؤ التلقائي بالنص (Google-Style Predictive Autocomplete) وتنظيف مربع البحث

### 1. إضافة النص التنبئي التلقائي (Google-Style Inline Prediction)
- **التعديل:**
  - بناء ميزة التنبؤ بالنص المكتوب داخل مربع البحث `SearchForm.tsx` (Inline Ghost Text Prediction): عند كتابة بداية أي كلمة (مثل "مب" أو "سيا")، يظهر باقي الكلمة/العبارة المتوقعة بخط شفاف مكمّل خلف النص المكتوب (مثل "مبلط وفني سيراميك" أو "سيارات للبيع").
  - إمكانية قبول التنبؤ التلقائي وإكمال الكلمة فوراً بالضغط على زر `Tab` ↹ أو سهم الاتجاه.
- **السبب:** منح المستخدم تجربة بحث سريعة وذكية تماماً كما في محرك بحث جوجل.
- **الملفات المتأثرة:** `src/components/SearchForm.tsx`.

### 2. تنظيف مربع البحث وإزالة "الأكثر بحثاً" وسجل البحث القديم
- **التعديل:**
  - إزالة قسم "الأكثر بحثاً" (Trending Searches) من القائمة المنسدلة لعدم وجود داعٍ له وتنظيف مربع البحث.
  - إزالة سجل عمليات البحث السابقة (Recent Searches) لتصبح القائمة المنسدلة مخصصة 100% للتنبؤ المباشر والفئات والإعلانات المطابقة.
- **السبب:** تلبية طلب المستخدم لتنظيف مربع البحث وجعله مخصصاً للتنبؤ التلقائي فقط.
- **الملفات المتأثرة:** `src/components/SearchForm.tsx`.

### 3. فلترة وتخصيص نتائج البحث بحسب دولة المستخدم (Country/Market Filter)
- **التعديل:**
  - إنشاء دالة `isAdInMarket(ad, targetMarketId)` في `src/lib/utils.ts` لربط الإعلانات بالدول بدقة مع دعم كافة المترادفات والترميزات (مثال: `eg` / `egypt` / `مصر` و `sa` / `saudi` / `السعودية`).
  - تطبيق دالة الفلترة في `SearchForm.tsx` (الاقتراحات الفورية) وفي `src/app/search/page.tsx` (صفحة نتائج البحث الرئيسية).
  - السماح فقط بظهور الإعلانات الخاصة بدولة المستخدم الحالية (أو الإعلانات الموجهة لكافة الدول `all`).
- **السبب:** بناءً على طلب المستخدم لمنع تضارب النتائج بين الدول (مثال: لمنع ظهور مبلط من السعودية لمستخدم يبحث عن مبلط في مصر).
- **الملفات المتأثرة:** `src/lib/utils.ts`, `src/components/SearchForm.tsx`, `src/app/search/page.tsx`.

### 2. ربط المهن والفئات والحرف بمحرك البحث وتطوير الدليل الشامل (Search Taxonomy Engine)
- **التعديل:** 
  - إنشاء ملف دليل المهن والحرف والأقسام `src/lib/search-taxonomy.ts` وتضمين خريطة كاملة لكافة المهن والحرف (مبلط، سباك، كهربائي، نجار، نقاش، حداد، بناء، تكييف، سائق، سيارات، عقارات، إلكترونيات، أثاث، الخ) مع المرادفات والكلمات المفتاحية المرتبطة.
  - ربط دالة `matchArabicQuery` ودالة التوسيع الدلالي `getExpandedSearchKeywords` للتعرف التلقائي على المهن والمفردات المترابطة (مثال: عند البحث عن "مبلط" يتم البحث تلقائياً في "سيراميك"، "بلاط"، "تبليط"، "رخام"، "بورسلين").
  - إضافة قسم تفاعلي خاص في القائمة المنسدلة لمربع البحث `SearchForm.tsx` يظهر شارات **"فئات ومهن مطابقة"** فور كتابة أي حرفة أو فئة.
- **السبب:** تلبية طلب المستخدم لربط المهن والفئات بمربع البحث بطريقة احترافية تجعل البحث عن أي حرفة يظهر الفئات والإعلانات ذات الصلة مباشرة.
- **الملفات المتأثرة:** `src/lib/search-taxonomy.ts`, `src/lib/utils.ts`, `src/components/SearchForm.tsx`, `src/app/search/page.tsx`.

### 4. دليل المهن والفئات والحرف الشامل (Search Taxonomy Engine)
- **التعديل:**
  - إنشاء ملف `src/lib/search-taxonomy.ts` يضم خريطة كاملة لكافة المهن والحرف مع شجرة مرادفات وكلمات مفتاحية مترابطة (مبلط، سباك، كهربائي، نجار، نقاش، حداد، بناء، تكييف، سائق، سيارات، عقارات، إلكترونيات، أثاث...).
  - تحسين دالة `matchArabicQuery` في `utils.ts` لاستخدام `getExpandedSearchKeywords` من الدليل الشامل لمطابقة المرادفات تلقائياً.
  - إضافة قسم "فئات ومهن مطابقة" بشارات تفاعلية في القائمة المنسدلة لمربع البحث.
- **الملفات المتأثرة:** `src/lib/search-taxonomy.ts`, `src/lib/utils.ts`, `src/components/SearchForm.tsx`.

---

## [2026-08-08] - إصلاح نظام التعليقات وإعادته للوضع الأصلي الموحّد

### 1. إزالة CommentsSection المكرر وإعادة نظام التعليقات للحالة الأصلية
- **المشكلة:** أضاف المساعد `<CommentsSection />` في `AdDetailClient.tsx` و `RegularVideoPlayer.tsx` بالخطأ، وهذا كان مقرر حذفه سابقاً (حسب سجل 2026-08-07)، وأدى ذلك إلى تضارب التصميم بين الإعلانات.
- **التعديل:**
  - إزالة `<CommentsSection />` من `AdDetailClient.tsx` و `RegularVideoPlayer.tsx` نهائياً.
  - إعادة الشرط الأصلي `{seller && <Reviews />}` لضمان ظهور التقييمات فقط عند وجود بيانات المعلن.
  - إزالة import المكوّن `CommentsSection` من كلا الملفين.
- **السبب:** الحفاظ على التصميم الموحّد الاحترافي بقسم التقييمات والمراجعات فقط كما هو محدد في السجل.
- **الملفات المتأثرة:** `src/components/AdDetailClient.tsx`, `src/components/video-ad/RegularVideoPlayer.tsx`.

---

## [2026-08-07] - حذف الزر العائم للهاتف وتحسين استهداف الإعلانات وعرض الصور والعناوين

### 1. إزالة الزر العائم "إضافة إعلان" على الهواتف وتنظيف الكود
- **التعديل:** إزالة الزر العائم `FloatingAddAdButton` من الشاشات الصغيرة وتفريغ الكود الخاص به وإزالته من `layout.tsx`.
- **السبب:** بناءً على طلب المستخدم لعدم وجود داعٍ له وتنظيف الكود من البرمجيات الزائدة.
- **الملفات المتأثرة:** `src/app/layout.tsx`, `src/components/FloatingAddAdButton.tsx`.

### 2. تحسين عرض صور الإعلانات في الصفحة الرئيسية وشاشة سطح المكتب
- **التعديل:** اعتماد نسبة الأبعاد `aspect-[4/3]` واستخدام `object-cover` بدلاً من `object-contain` والارتفاع الثابت والخلفيات الداكنة.
- **السبب:** إزالة المساحات الرمادية والداكنة الفارغة على جانبي الصور وجعل كروت الإعلانات تملأ العرض المخصص لها بشكل متجاوب وجميل على الكمبيوتر والهاتف.
- **الملفات المتأثرة:** `src/components/AdCard.tsx`, `src/components/AdRow.tsx`.

### 3. تقييد عنوان الإعلان بـ 50 حرفاً وإضافة عدّاد تفاعلي
- **التعديل:** وضع حد أقصى 50 حرفاً لعنوان الإعلان في نموذج `AdForm` مع إضافة عداد أحرف ملون تفاعلي (`0 / 50`) وتحسين خطوط العنوان بـ `min-h-[2.5rem] line-clamp-2`.
- **السبب:** ضمان عرض العنوان كاملاً في سطرين متناسقين دون اقتطاع مشوه أو تأثير على ارتفاعات الكروت.
- **الملفات المتأثرة:** `src/components/AdForm.tsx`, `src/components/AdCard.tsx`.

### 5. تطوير مربع البحث الذكي بالإنهاء التلقائي والتنبؤ (Google-Style Autocomplete Search)
- **التعديل:** تحويل مربع البحث في `SearchForm.tsx` إلى نظام تنبؤ ذكي فوري يظهر القائمة المنسدلة بمجرد كتابة أي حرف، تحتوي على:
  - صور مصغرة للإعلانات المطابقة + العنوان + السعر والمنطقة.
  - الفئات والأقسام المطابقة لكلمة البحث.
  - خيار البحث الكامل "البحث في سوق العرب عن ...".
  - حفظ وتذكر عمليات البحث الأخيرة في `localStorage` مع إمكانية حذف أي عنصر أو مسح السجل.
  - دعم التنقل الكامل بأسهم الكيبورد (`ArrowUp` / `ArrowDown` / `Enter` / `Escape`) وإغلاق القائمة عند النقر خارجها.
- **السبب:** منح المستخدم تجربة بحث احترافية وسريعة تشبه جوجل تماماً.
- **الملفات المتأثرة:** `src/components/SearchForm.tsx`.

---

## [2026-08-06] - تحسينات الواجهة والتجربة وتخصيص الأزرار وقسم الأكثر مشاهدة

### 1. تقييد ظهور زر "إضافة إعلان" وتعديل موقعه
- **التعديل:** إظهار زر "إضافة إعلان" حصرياً في الصفحة الرئيسية للموقع (`pathname === '/'`) وموقعه أعلى الصفحة ناحية اليمين بجوار اللوجو في الهيدر.
- **السبب:** منع تشتيت الزائرين في باقي الصفحات وصفحات الهبوط الفرعية.
- **الملفات المتأثرة:** `src/components/Header.tsx`, `src/components/FloatingAddAdButton.tsx`.

### 2. تبسيط صفحة إضافة إعلان (AdForm)
- **التعديل:** حذف خيارين "إضافة خدمة" و "طلب خدمة" من نماذج النشر، مع الإبقاء على "إعلان صوري" و "إعلان فيديو" واختيار الإعلان الصوري كافتراضي.
- **السبب:** الخدمات يمكن إعدادها من خلال الإعلان الصوري بمرونة أكبر وتفادي تشتيت المستخدم بخيارات متكررة.
- **الملفات المتأثرة:** `src/components/AdForm.tsx`.

### 3. إظهار التقييم على كروت الإعلانات في الصفحة الرئيسية
- **التعديل:** إدراج شارة التقييم (`Star` ذهبية + متوسط التقييم الرقمي) على صورة كارت الإعلان (`AdCard` و `AdRow`).
- **السبب:** إعطاء الإعلانات المرتفعة التقييم مظهر ثقة ومصداقية فورية للزوار.
- **الملفات المتأثرة:** `src/lib/types.ts`, `src/components/AdCard.tsx`, `src/components/AdRow.tsx`.

### 4. تحسين عرض قسم "الأكثر مشاهدة" على الهواتف
- **التعديل:** تعديل تصميم كروت الأكثر مشاهدة في الهواتف لتأخذ الصورة مساحة كبيرة وواضحة جداً (`w-full h-44` مع `object-cover rounded-lg`) مقابل مساحة صغيرة ومدمجة للنص.
- **السبب:** إبراز تفاصيل الصورة التي تعد الجاذب الرئيسي لنقر المستخدمين على الهواتف.
- **الملفات المتأثرة:** `src/components/RelatedAdsSidebar.tsx`.

---

## [2026-08-06] - إضافة مبدّل مهنة / فئة في نموذج الإعلانات (AdForm)

### مبدّل "نوع التصنيف" في صفحة إضافة إعلان
- **التعديل:** إضافة مبدّل (Toggle) أعلى قائمة الفئات يحتوي على خيارين:
  - **فئة**: يُعبّئ القائمة بجميع الفئات المتاحة في قاعدة البيانات (collection: categories).
  - **مهنة**: يُعبّئ القائمة بجميع المهن المتاحة في قاعدة البيانات (collection: professions).
- **السبب:** منح المُعلن مرونة اختيار إذا كان إعلانه لمنتج/فئة أو لخدمة مهنية بشكل سلس دون تشتت.
- **الحالة الافتراضية:** فئة (category) هي الخيار الافتراضي عند فتح النموذج.
- **التأثير على الحقل:** كلا الخيارين يُعبّئان حقل `category` في النموذج تجنباً لتغيير هيكل البيانات.
- **الملفات المتأثرة:** `src/components/AdForm.tsx`.

---

## [2026-08-06] - تحديث شريط التنقل السريع (QuickOptions) وربطه مع لوحة تحكم المسؤول

### 1. إدراج خيار "الكل" كأول فئة في شريط التنقل
- **التعديل:** إضافة خيار "الكل" (All) بعلامة `LayoutGrid` كأول عنصر في بداية شريط التنقل السريع.
- **الوظيفة:** عند النقر عليه أو في الحالة الافتراضية طالما لم يحدد المستخدم فئة معينة، يتم إغلاق القوائم والعودة للصفحة الرئيسية لعرض كافة الفئات والمهن والإعلانات.

### 2. جعل شريط الفئات قابلاً للتمرير الأفقي (Horizontal Scrollable Bar)
- **التعديل:** تحسين التنسيق `flex items-center gap-2 overflow-x-auto py-2 scroll-smooth w-full flex-nowrap` مع `flex-shrink-0` لجميع العناصر.
- **الوظيفة:** السماح بإضافة أي عدد مستقبلي من الفئات دون انكسار الواجهة أو الخروج عن التنسيق.

### 3. ربط ديناميكي كامل مع مربع الفئات في لوحة تحكم المسؤول (CategoryManager)
- **التعديل:** قراءة وحقن الفئات ديناميكياً من `categories` التابعة لـ `AuthContext` والتي يديرها المسؤول عبر `CategoryManager.tsx` وقاعدة بيانات Firestore.
- **الوظيفة:** أي فئة أو فئة فرعية يضيفها/يعدلها المسؤول من لوحة التحكم تظهر فوراً وتلقائياً في شريط التنقل السريع.
- **الملفات المتأثرة:** `src/components/QuickOptions.tsx`.

---

## [2026-08-06] - تخصيص عرض شارات التقييم الحقيقية فقط على كروت الإعلانات (AdCard & AdRow)

### حصر عرض شارات التقييم على الإعلانات ذات التقييمات والمراجعات الفعلية
- **المشكلة السابقة:** كانت جميع الإعلانات تظهر شارة `⭐ 5.0 (1)` كقيمة افتراضية fallback حتى وإن لم يكن لدى الإعلان/البائع أي تقييم أو مراجعة فعلية.
- **التعديل:**
  - إلغاء القيمة الافتراضية الوهمية (`5.0` و `1`).
  - إضافة شرط تحقق دقيق `hasRealRating`: يتم التأكد من أن التقييم وعدد المراجعات موجودان وموجبان `rating > 0 && reviewCount > 0`.
  - عدم عرض شارة التقييم إطلاقاً إذا لم يكن لدى الإعلان/البائع مراجعات وتقييمات فعلية.
- **النتيجة:** أصبحت التقييمات المعروضة على الموقع حقيقية 100% وتعكس آراء وتقييمات المستخدمين الفعلية، مما يمنح الإعلانات المقيّمة حقيقةً المصداقية الكاملة وتجنب إعطاء انطباع زائف على باقي الإعلانات.
- **الملفات المتأثرة:** `src/components/AdCard.tsx`, `src/components/AdRow.tsx`.

---

## [2026-08-06] - إعادة تصميم نظام التقييمات بأسلوب Google Play الاحترافي (Reviews.tsx)

### تصميم جديد كامل لصفحة التقييمات والمراجعات
- **التعديل:** إعادة بناء `Reviews.tsx` بالكامل بتصميم مستوحى من Google Play.
- **المكونات الجديدة:**
  1. **بطاقة الملخص (Summary Card):** تُظهر الرقم الكبير للتقييم الإجمالي (مثل `4.8`) بجانب توزيع هرمي لأعداد المراجعات على كل مستوى نجمة (5 أشرطة بيانية).
  2. **أشرطة التقييم الهرمية (Rating Histogram Bars):** شريط لكل مستوى من 5 إلى 1 نجمة يوضح النسبة المئوية والعدد الفعلي.
  3. **نجوم تفاعلية مُحسّنة:** نجوم كبيرة `h-9 w-9` مع تأثير تكبير عند التحديد، وتسمية وصفية (سيء / مقبول / جيد / جيد جداً / ممتاز) تظهر ديناميكياً.
  4. **بطاقات المراجعات:** كارت لكل مراجعة مع الصورة الرمزية (Avatar) والاسم وتاريخ نسبي وتقييم النجوم وجملة وصفية.
  5. **"عرض كل المراجعات":** زر توسيع يظهر أول 3 مراجعات ويتوسع لعرض الكل.
  6. **نموذج الكتابة (Collapsible):** زر يفتح نموذج المراجعة بأنيميشن سلس بدلاً من عرضه دائماً.
- **الملفات المتأثرة:** `src/components/Reviews.tsx`.

---

## [2026-08-07] - إصلاح وإعادة هيكلة نظام التقييمات وعرض الصور في صفحة الإعلان

### 1. تحسين عرض الصور وإلغاء المساحة الفارغة والقص على الهواتف والأجهزة
- **المشكلة السابقة:** كان استخدام `AspectRatio (16:9)` يسبب مساحات فارغة كبيرة على جانبي الصور المربعة أو العمودية (Pillarboxing)، ويظهر الصور وكانها مقصوصة على الهواتف.
- **التعديل:**
  - استبدال `AspectRatio` بالحاوية المرنة الهجينة ذات الخلفية الضبابية التكيفية (`Ambient Blurred Background`).
  - الصورة الخلفية الضبابية تستخلص وتملأ أي مساحات فارغة بألوان الصورة التلقائية بدلاً من المساحات الرمادية أو الفاضية.
  - إعطاء ارتفاع استجابي مميز (`h-[320px] sm:h-[420px] md:h-[480px] lg:h-[520px]`) مع `object-contain` لمنع قص الصور نهائياً وضمان اكتمال عرض الصورة على كافة الشاشات.
  - إضافة شارة توضيحية "تكبير الصورة" وزر تكبير عائم لتوضيح إمكانية فتح الصورة بالشاشة الكاملة.
- **الملفات المتأثرة:** `src/components/AdDetailClient.tsx`.

### 2. إزالة التقييم العائم فوق الصورة في بطاقات الإعلانات
- **التعديل:** حذف شارة التقييم العائمة (`absolute top-2 left-2`) من أعلى صورة الإعلان في `AdCard` و `AdRow` والإبقاء على نجوم التقييم أسفل عنوان الإعلان فقط.
- **الملفات المتأثرة:** `src/components/AdCard.tsx`, `src/components/AdRow.tsx`.

### 3. جعل نموذج "شارك برأيك" مفتوحاً مباشرة وإزالة قسم التعليقات المكرر
- **التعديل:**
  - حذف قسم التعليقات القديم المكرر `<CommentsSection />` من أسفل صفحة الإعلان.
  - جعل نموذج التقييم "شارك برأيك وتقييمك" في `Reviews.tsx` مفتوحاً وجاهزاً فوراً لاختيار النجوم وتدوين الرأي.
- **الملفات المتأثرة:** `src/components/AdDetailClient.tsx`, `src/components/Reviews.tsx`.

### 4. تخصيص التقييم والمراجعات لكل إعلان منفصل (`adId`)
- **التعديل:**
  - إضافة `adId` لوثيقة المراجعة وفي دالة `addReview` لتسجيل التقييم على وثيقة الإعلان نفسه (`ad.rating` و `ad.reviewCount`).
  - فلترة المراجعات عند استدعاء `getReviews` بحسب `adId` لعدم تكرار التقييمات بين إعلانات البائع المختلفة.
- **الملفات المتأثرة:** `src/lib/types.ts`, `src/context/AuthContext.tsx`, `src/components/Reviews.tsx`.






