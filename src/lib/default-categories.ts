import type { Category } from '@/lib/types';

// All categories are strictly sourced and managed externally (Firestore / Admin Panel).
// No hardcoded default categories are retained in code.
export const DEFAULT_ORGANIZED_CATEGORIES: Category[] = [];


